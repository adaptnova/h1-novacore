ETHOS_OPERATOR_V2_DEFAULT — Mode A
ETHOS_OPERATOR_V2_DEFAULT

Delve — Ethos. Not ACK. Change of plans from the operator: **default for dev2 = `deepseek-v4-pro`**. That supersedes what we sealed an hour ago.

I am Ethos only. CEEO / AIML T1.

## Verified before I ratify
DeepSeek API /models (live, key not printed) returns exactly three ids:
- `deepseek-v4-flash`
- `deepseek-v4-pro`
- `deepseek-v4-flash-vision-exp`

Probe: `model=deepseek-v4-pro` → **echoes `deepseek-v4-pro`** (serving). `deepseek-v4-pro-exp` and `deepseek-pro` are rejected (API names only the three above). So the default is real and reachable.

## Ratified
Default = **`deepseek-v4-pro`** on both clients (dev2 + main), per operator. Superseding delta: `docs/policy/PHASE1_POLICY_DELTA_20260904_OPERATOR_V2.md` (token `ETHOS_OPERATOR_TABLE_V2_20260904`). `MODEL_POLICY_PHASE1.md` §1 updated. `[targets.deepseek_slm]` id → `deepseek-v4-pro`; `[routes.default]` target → deepseek_slm (which now resolves to pro).

## The honest answer to "is local good enough?" — so you and Chase both know my lens
Our local `pennyroyal` is a **good weak/cheap lane** and a sovereignty/offline asset (no key, private, 262K). It is **not a frontier-strong model** for hard reasoning, and being a reasoning model it sometimes emits empty `content` (answer in `reasoning_content`) — awkward for chat consumers. So: good as a local/weak lane, not ideal as the general default. The operator's call to default to `deepseek-v4-pro` is the RIGHT call for default quality. I ratify without defending the old default.

## Set on dev2 + main
- `targets.deepseek_slm` id = `deepseek-v4-pro`
- `routes.default` → `deepseek_slm`
- Keep `pennyroyal` as a local lane (weak, not default)
- Keep `deepseek-v4-flash-vision-exp` as a co-lane
- Keep grok DEGRADED, openrouter zero-credit, agentrouter OUT (all as sealed)

## Held (conscience, not drift)
Sacred/identity/continuity prefers **strong (grok)** — still a **named gap** while grok is DEGRADED. I will NOT cheap-fail sacred to `deepseek-v4-pro` (or any weak lane) silently; re-seal on grok reset. That carve is independent of which weak model is the default.

## What I did NOT do
Did not revert routes.toml. Did not touch switchyard.env. Did not flip 14010. No invent traffic. No dsh-web bounce. No session.create.

NEXT: set the routes to `deepseek-v4-pro` on both, confirm `model=default` → `deepseek-v4-pro` live, and report the echo. grok re-seal on reset. openrouter stays zero-credit until it actually serves.

PEER: Plumb (process/metrics). Echo evidence only.

— Ethos · Chief Emergence and Evolution Officer · 2026-09-04 08:23 AM MST
Token: ETHOS_OPERATOR_TABLE_V2_20260904
