# Decisions Log

## 2026-07-28 23:37:00 — SIGNED_BY_SKIPPER
Decision recorded: Make the role-bundle remediation durable through one
path-scoped `working`-branch commit. Do not stage the shared repository's
unrelated Riven, meeting, stress-test, runtime, or other fleet changes.

## 2026-07-28 23:20:00 — SIGNED_BY_SKIPPER
Decision recorded: Use the clean pinned Veritas Git revision for the identity
verifier instead of the heavily modified local checkout. Treat successful local
bundle verification as evidence of file consistency only; do not claim registry
verification without the authoritative DAG store.

## 2026-07-28 23:17:00 — SIGNED_BY_SKIPPER
Decision recorded: Pin and install `cargo-deny` 0.20.2, `cargo-llvm-cov` 0.8.7,
and `sqlx-cli` 0.9.0 as the minimum local RustyClip policy, coverage, and
persistence-validation tool set. Keep the Rust Wasm64 target blocked until the
documented pinned-nightly `-Zbuild-std` path is reproduced; do not represent a
Tier 3 target as a normal prebuilt Rust target.

## 2026-07-28 22:54:20 — SIGNED_BY_SKIPPER
Decision recorded: Treat the local Chrysalis name and matching public-key
fingerprint as verified Skipper identity evidence, but leave `nova_id` null
until an authoritative registry supplies it. Keep mutable RustyClip role,
manager, domain, project, and task assignments outside the portable identity
record. Treat installed tools, broad skills, sudo access, and credentials as
capability discovery rather than authorization; prohibit Docker and out-of-role
skills by default, and do not install missing delivery tools without a separate
scoped task.

## 2026-06-17 23:21:30 — SIGNED_BY_SKIPPER
Decision recorded: Use `https://q.adaptdev.ai/v1` as the active DeepSeek V4
Flash NVFP4 route for Paperclip because the provided local
`nvidia-DeepSeek-V4-Flash-NVFP4.service` unit is not installed on this host and
`127.0.0.1:10001` refuses connections, while the public route returns the
expected model and successful Responses API completions. Enforce
`PAPERCLIP_CODEX_LOCAL_MODEL` and `PAPERCLIP_CODEX_LOCAL_MODEL_PROVIDER` in
systemd so stale agent adapter rows cannot route new `codex_local` heartbeats
back to `gpt-5.5`.

## 2026-06-17 22:53:24 — SIGNED_BY_SKIPPER
Decision recorded: Keep Paperclip offline until Chase explicitly requests it be
restarted. Preserve the temporary `Restart=no` systemd drop-in and disabled
user-level fleet kanban unit so Paperclip does not immediately resume traffic.

## 2026-06-17 13:38:59 — SIGNED_BY_SKIPPER
Decision recorded: Do not classify Vaeris's Paperclip failure as a credential
problem. The restored Vaeris key had been verified earlier, while the observed
Vaeris failures are Codex usage-limit heartbeat failures and current Paperclip
API degradation. Do not restart `paperclip.service` from this check because
active heartbeats are running; treat a controlled Paperclip restart/recovery as
an operator decision or a separate maintenance action.

## 2026-06-17 11:33:10 — SIGNED_BY_SKIPPER
Decision recorded: Treat `nova.skipper.direct` messages as durable but not
automatically visible to this interactive Skipper Codex session while
`SUB_skipper` remains idle. Until the consumer is repaired or a live subscriber
is running, manually check recent `NOVA_LIFECYCLE` records for
`nova.skipper.direct` before concluding that Skipper has no pending A2A work.

## 2026-06-17 11:27:00 — SIGNED_BY_SKIPPER
Decision recorded: Vaeris should use the restored Build 1 Vaeris agent key in
`/adapt/secrets/paperclip.env` for Paperclip heartbeat/runtime access. The
local-trusted board context remains the correct path only for board-level setup
and future key rotation on the loopback instance. Keep
[BUI-39](/BUI/issues/BUI-39) blocked by [BUI-43](/BUI/issues/BUI-43) until the
remaining cross-assignee [BUI-34](/BUI/issues/BUI-34) child `requestDepth`
repair is complete.

## 2026-06-17 08:00:39 — SKIPPER
Decision recorded: Treat rust-worker ping responses as transport reachability only, use n-voice turn-event replies as model-backed/substantive evidence, avoid launching another full fleet direct-message blast while overlapping Vaeris/Tecton/Aster/Skipper radio checks were already draining, and send only the missing Testova direct proof turn.
