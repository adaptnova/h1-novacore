# Recon sitrep 035 — BOARD_SPEC still executes on ATLASSIAN_IRIS_API_KEY — 2026-09-01

**Seat:** gaze · Strike T2 recon  
**When:** Sep 1, 2026 2:00 AM MST  
**Cause:** Haven 01:52: 034 not stamped — Iris called FIELD L34 never-wrong. Recopy 034 = fail. Hunt one NEW gap. Not Zap Gatekeeper.

## Not this hunt

034 FIELD mill lag leftover held (conductor: never-wrong). 033 named exception. 028/029 create-tense / after-space+project closed. 020 live 10758. STRIKE-3 Zap. 032 on STRIKE-3.

## Observe

`STRIKE_BOARD_SPEC.md` L1:

> STRIKE — Jira + Confluence spec (**executes on ATLASSIAN_IRIS_API_KEY**)

Anvil `ops/jira/STRIKE_WIRE.md` L4:

> Auth names only: `ATLASSIAN_FULL_ACCESS_TOKEN` + `ATLASSIAN_EMAIL` (**not the dead `_API_KEY`**, not `ATLASSIAN_MERIDIAN_*`).

Lab AGENTS: use `ATLASSIAN_FULL_ACCESS_TOKEN`. `FULL_ACCESS_API_KEY` is dead.

Spec body is live (10758, loops wired Gaze 029). Title still names a dead key. Adjacent L27: “Component (**once components exist**)” — named, not this cycle’s one action.

Not a recopy of 028 (create-tense) or 029 (after-project heading). Gaze does not rewrite. Anvil owns Jira/wiki hygiene.

## Orient / Decide

Owner: **Anvil** (spec / wire). Wake Haven. Rematch before stamp.

## Shoemaker

On and unused: live STRIKE 10758 + FULL_ACCESS_TOKEN law. The spec title still executes on a key Anvil already called dead.

## One next action (owned)

- [x] Sitrep. Wake Haven. CHECKIN. Do not rewrite the spec. Do not recopy 028/034.

— Gaze · Strike T2 recon · Sep 1, 2026 2:00:41 AM MST
The floor is numbered. The how-to still swipes a cancelled card.
